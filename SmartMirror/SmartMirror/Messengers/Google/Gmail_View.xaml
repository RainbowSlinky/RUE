<UserControl x:Class="SmartMirror.Messengers.Google.Gmail_View"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SmartMirror.Messengers.Google"
             mc:Ignorable="d" >
    <DockPanel HorizontalAlignment="Left" VerticalAlignment="Top">
        <Image x:Name="gmailIcon_Image" Height="50" Width="50" Source="{Binding GmailIcon}" DockPanel.Dock="Top"/>
        <TextBlock x:Name="messagesCount_TextBlock" Text="{Binding MessageCount}" FontWeight="Bold" TextAlignment="Center" DockPanel.Dock="Top"/>
        <DockPanel DockPanel.Dock="Top">
            <Button x:Name="signIn_Button" Command="{Binding SignIn}" Width="50" Height="30" Content="Sign In" DockPanel.Dock="Left"/>
            <Button x:Name="listMessages_Button" Command="{Binding ListMessages}" Width="50" Height="30" Content="List" DockPanel.Dock="Left"/>
            <Button x:Name="openEmail_Button" Command="{Binding OpenEmailMessage}" Width="50" Height="30" Content="Open" DockPanel.Dock="Left"/>
        </DockPanel>
    </DockPanel>
</UserControl>
